# Modelo de configuracao. Copie este arquivo para ".env" e preencha o token.
#
# Como gerar o token:
#   GitHub > Settings > Developer settings > Personal access tokens
#   > Tokens (classic) > Generate new token
#   Para minerar repositorios publicos basta o escopo "public_repo".
#   Para o snapshot do board (npm run snapshot) e preciso marcar tambem
#   o escopo "read:project" - sem ele a API recusa a consulta ao Projects v2.
#
# O arquivo .env esta no .gitignore e NAO pode ser enviado para o repositorio.
GITHUB_TOKEN=coloque_seu_token_aqui
